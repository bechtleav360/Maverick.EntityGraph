#spring:
#  datasource:
#    url: jdbc:h2:file:/data/demo

security:
  apiKey: "1234"
  basic:
    enabled: false

storage:
  default:
    path: "/mnt/d/repositories"
  entities:
    path: "/mnt/d/Graphen/entities"
  transactions:
    path: "/mnt/d/Graphen/transactions"
  schema:
    path: "/mnt/d/Graphen/schema"

spring:
  security:
    user:
      name: "user"
      password: "password"

logging:
  level:
    com:
      bechtle: TRACE
    org:
      springframework:
        security: INFO


management:
  endpoints:
    web:
      exposure:
        include: "health, info, env, loggers, metrics, scheduledTasks, logfile, mappings"
  endpoint:
    health:
      enabled: true
    info:
      enabled: true
    env:
      enabled: true
    loggers:
      enabled: true
    metrics:
      enabled: true
    scheduledTasks:
      enabled: true
    logfile:
      enabled: true